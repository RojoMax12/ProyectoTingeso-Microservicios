server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/RojoMax12/ProyectoTingeso-Microservicios/ToolRent-Backend
          search-paths: config-data
  application:
    name: config-service
